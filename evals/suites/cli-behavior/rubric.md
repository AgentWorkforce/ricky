# CLI Behavior Rubric

Use this suite for deterministic command-surface behavior.

## Pass Bar

The command should complete without stack traces, preserve scriptable output, and
name only implemented Ricky surfaces. CLI evals should avoid broad product
judgment; use workflow-authoring cases when the quality bar depends on reading a
plan or generated workflow.
